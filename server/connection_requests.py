# Here we put constants that are used in payload to denote different types
# of connection requests.

discover_server = 'DISCOVERY'.encode('utf8')
game_update = 'GAME_UPDATE'.encode('utf8')
disconnect_server = 'DISCONNECT'.encode('utf8')
